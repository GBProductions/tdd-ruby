    @alphabet = [/[abcdefghijklmnopqrstuvwxyz]/i]

      def alphabet
    @alphabet
  end
end

class Anagram2
  def initialize2(word2)
    @word2 = word2
  end

  def word2
    @word2



word_array =@word.split('')
word_array.each do |character|
# puts 'Enter a new word'
# input = gets.chomp
# new_game = RPS.new(input)

  it('initializes alphabet class') do
    test = Anagram.new('')
    expect(test.alphabet).to(eq([/[abcdefghijklmnopqrstuvwxyz]/i]))
    end
  end

    it('initializes alphabet class') do
    test = Anagram.new([/[abcdefghijklmnopqrstuvwxyz]/i])
    expect(test.alphabet()).to(eq([/[abcdefghijklmnopqrstuvwxyz]/i]))
    end
  end

    def alphabet
    @alphabet
  end

      @alphabet = [/[abcdefghijklmnopqrstuvwxyz]/i]

  def anagrams
    word_array1 = @word1.split('')
    word_array2 = @word2.split('')
    word_array1.each do |character|
      if character.match(word_array1)
      return "These words are anagrams!"
  end
